# action-docker-compose (deprecated)

This repository is **deprecated**. The composite action has been migrated to [emo-workflows](https://github.com/emocional/emo-workflows).

Use instead:

```yaml
uses: emocional/emo-workflows/.github/actions/docker-compose@main
```

Source: `.github/actions/docker-compose/action.yml` in `emo-workflows`.
